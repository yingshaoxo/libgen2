import { exposeInMainWorld } from './exposeInMainWorld';


// import { create, type IPFSHTTPClient, type Options } from 'ipfs-http-client';

// Export for types in contracts.d.ts
// export const ipfsClientModule = { create } as const;

// exposeInMainWorld('ipfsClientModule', ipfsClientModule);
